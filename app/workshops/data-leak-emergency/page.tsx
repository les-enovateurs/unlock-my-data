import UrgenceFuite from "@/components/ateliers/UrgenceFuite";

export const metadata = {
    title: "My data was leaked? React and secure | Unlock My Data",
    description: "Interactive step-by-step guide to secure your accounts after a data breach.",
};

export default function DataLeakEmergencyPage() {
    return <UrgenceFuite lang="en" />;
}
